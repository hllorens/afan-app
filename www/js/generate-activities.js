// pillar un csv i generar un json amb respostes pre-calculades
$(function () { // DOM ready
	// parse csv
	
	// generate a super structure that contains all available words classified by sounds they contain, num of syllabs, level
	// queriable
	// [a]
	
	// or just with the list big for checking one by one
	// e.g., k a s a, level 1
	//--> 2 consecutive equal leters
	// differentiate vowels (easy to distinguish) and consonants (classify by conflictive ones s-ss c-ss p-b ny-ll-y-g d-t l-r ), sequence pra-par ... same consonants/vowels but different order
	
	// for each word generate n activities
	
});
